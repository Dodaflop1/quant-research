# Core numerics / data
numpy>=1.26,<3
pandas>=2.2,<3
pyarrow>=15

# Storage
SQLAlchemy>=2.0,<3
duckdb>=0.10
# NOTE: sqlite3 is in the standard library - no dependency required.

# API clients
aiohttp>=3.9,<4
praw>=7.7,<8
requests>=2.31,<3
python-dotenv>=1.0
cryptography>=42          # RSA-PSS request signing for Kalshi
# NOTE: asyncio is in the standard library - the PyPI package of that name is a
# dead backport for Python 3.3 and must not be installed.

# Statistics / modelling
scipy>=1.11,<2
scikit-learn>=1.3,<2
statsmodels>=0.14,<1
# NOTE: the `tick` library is the usual off-the-shelf Hawkes implementation, but it
# is effectively unmaintained and does not build on current Python. The Hawkes MLE
# here is written directly against scipy.optimize - see docs/diffusion_methodology.md.

# Validation
pydantic>=2.6,<3
pydantic-settings>=2.1,<3

# Visualisation
matplotlib>=3.8,<4
seaborn>=0.13
plotly>=5.18
streamlit>=1.30

# Utilities
python-dateutil>=2.8
tqdm>=4.66
yfinance>=0.2,<1
openai>=1.0,<3
# NOTE: timezone handling uses the stdlib zoneinfo module (Python >= 3.9), not pytz.
